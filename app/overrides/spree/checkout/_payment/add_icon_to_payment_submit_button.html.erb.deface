<!-- replace 'code:contains("submit_tag t(:save_and_continue)")' -->
<%= button_tag content_tag(:i, '', :class => 'icon-right-bold') + ' ' + t(:save_and_continue), :type => 'submit', :class => 'continue button primary' %>